import { SplideConfig } from '../types/config';

export const initialConfig: SplideConfig = {
  type: "loop",
  height: "400px",
  perPage: 3,
  perMove: 1,
  gap: "1rem",
  arrows: true,
  pagination: true,
  drag: true,
  rewind: false,
};
